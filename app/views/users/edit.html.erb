<% if @user.new_record? %>
<h1>Editing User</h1>
<% else %>
<h1>Change password of <%= @user.username %></h1>
<% end %>

<%= render 'form' %>

<%= link_to 'Show', @user, class:'btn btn-primary' %> |
<%= link_to 'Back', users_path, class:'btn btn-primary' %>
